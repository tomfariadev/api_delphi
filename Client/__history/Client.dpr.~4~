program Client;

uses
  Vcl.Forms,
  uMain in 'uMain.pas' {FMain},
  uAdicionar in 'uAdicionar.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TFMain, FMain);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
